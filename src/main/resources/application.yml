cloud:
  aws:
    credentials:
      access-key: __INSERT_ACCESS_KEY__
      secret-key: __INSERT_SECRET_KEY__

    region:
      static: us-east-2
    stack: 
      auto: false

application:
  bucket:
    name: revreimbursebucket

spring:
  servlet:
    multipart:
     enabled: true
     file-size-threshold: 2MB
     max-file-size: 5MB
     max-request-size: 10MB

server:
  port: 7777